{
  "name": "rtrace",
  "version": "0.1.0",
  "description": "A minimal Rust library with Node.js bindings",
  "main": "index.js",
  "types": "index.d.ts",
  "files": [
    "index.js",
    "index.d.ts",
    "tracer/",
    "schema/"
  ],
  "scripts": {
    "build": "npm run build:node && npm run build:copy",
    "build:node": "napi build --release --cargo-cwd bindings/node",
    "build:copy": "npm run schema:compile && cp index.d.ts tracer/ && cp rtrace.node tracer/ && cp dist/schema.js dist/schema.d.ts schema/ && cp scripts/barrel-index.d.ts index.d.ts",
    "test": "npm run build && node scripts/test.js",
    "example": "npm run build && node scripts/example.js",
    "hereby": "hereby",
    "task": "hereby",
    "tasks": "hereby --list",
    "schema:compile": "tsc",
    "schema:generate": "npm run schema:compile && node dist/validate-schema.js --generate-schema",
    "schema:validate": "npm run schema:compile && node dist/validate-schema.js --validate",
    "schema:all": "npm run schema:generate && npm run schema:validate"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SeaRyanC/rtrace.git"
  },
  "keywords": [
    "rust",
    "nodejs",
    "bindings"
  ],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/SeaRyanC/rtrace/issues"
  },
  "homepage": "https://github.com/SeaRyanC/rtrace#readme",
  "napi": {
    "name": "rtrace",
    "triples": {
      "defaults": true
    }
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.4",
    "@types/node": "^24.3.3",
    "hereby": "^1.11.0",
    "typescript": "^5.9.2",
    "zod": "^4.1.8"
  },
  "dependencies": {
    "pngjs": "^7.0.0"
  }
}
