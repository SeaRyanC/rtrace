{
  "name": "rtrace",
  "version": "0.1.0",
  "description": "A minimal Rust library with Node.js bindings",
  "main": "rtrace.node",
  "types": "index.d.ts",
  "exports": {
    ".": {
      "import": "./rtrace.node",
      "require": "./rtrace.node"
    },
    "./schema": "./schema/schema.js"
  },
  "files": [
    "rtrace.node",
    "index.d.ts",
    "schema/schema.js",
    "schema/schema.d.ts",
    "schema/validate-schema.js",
    "schema/validate-schema.d.ts"
  ],
  "scripts": {
    "build": "npm run build:node",
    "build:node": "napi build --release --cargo-cwd bindings/node",
    "test": "npm run build && node scripts/test.js",
    "example": "npm run build && node scripts/example.js",
    "hereby": "hereby",
    "task": "hereby",
    "tasks": "hereby --list",
    "schema:compile": "cd schema && npx tsc",
    "schema:generate": "npm run schema:compile && node schema/validate-schema.js --generate-schema",
    "schema:validate": "npm run schema:compile && node schema/validate-schema.js --validate",
    "schema:all": "npm run schema:generate && npm run schema:validate"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SeaRyanC/rtrace.git"
  },
  "keywords": [
    "rust",
    "nodejs",
    "bindings"
  ],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/SeaRyanC/rtrace/issues"
  },
  "homepage": "https://github.com/SeaRyanC/rtrace#readme",
  "napi": {
    "name": "rtrace",
    "triples": {
      "defaults": true
    }
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.4",
    "@types/node": "^24.3.3",
    "hereby": "^1.11.0",
    "typescript": "^5.9.2",
    "zod": "^4.1.8"
  },
  "dependencies": {
    "pngjs": "^7.0.0"
  }
}
